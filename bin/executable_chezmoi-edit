#!/usr/bin/env nu

def main [file: path] {
	with-env { EDITOR: nano } { chezmoi edit $file }
	cd ~/.local/share/chezmoi
	git add .
	try { git commit -m - }
	git push
	chezmoi update
	sshp -j 'chezmoi update'
}
