#!/usr/bin/env bash
set -uo pipefail
[[ -d .git ]] || git init
[[ -f .gitignore ]] || echo ".flags/local" > .gitignore
if command -v nu; then
	[[ -f main.nu ]] || new nu main.nu
elif command -v bash; then
	[[ -f main.sh ]] || new bash main.sh
else
	printf '[\e[91mERR\e[0m]: %s\n' \
		"'nu' or 'bash' shells could not be found!" \
		>&2
fi
